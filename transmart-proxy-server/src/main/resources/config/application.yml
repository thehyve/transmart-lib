
spring:
  application:
    name: transmart-proxy
  jackson:
    serialization:
      write_dates_as_timestamps: false

server:
  port: 9050

keycloak:
  bearer-only: true
  use-resource-role-mappings: true
